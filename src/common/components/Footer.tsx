export default function Footer() {
	return (
		<footer className="mt-20 border-t border-[var(--line)] px-4 py-6 text-center">
			<p className="m-0 text-xs text-[var(--sea-ink-soft)]">
				<span className="font-semibold">plan</span>
				<span className="text-[var(--lagoon)]">.md</span> — collaborative plan
				review
			</p>
		</footer>
	);
}
